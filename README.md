# TransactionManager
Angular 2 Maven Spring WebApp
Compile the code by executing the command
<pre>mvn clean install</pre> in project directory. Then deploy the war file <code>target/transactionManager-1.0.war</code> in your server.
